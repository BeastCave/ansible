---
- name: Remove dependencies that are no longer required
  ansible.builtin.apt:
    autoremove: true

- name: Install NFS
  ansible.builtin.apt:
    name: 
      - nfs-client
      - nfs-common

    
