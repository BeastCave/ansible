# config file for ansible -- https://ansible.com/
# ===============================================
[defaults]
remote_user = ansible
inventory = ./inventory/inventory.yaml
private_key_file = ~/.ssh/primary.key
host_key_checking = False
remote_tmp = /tmp

[privilege_escalation]
become = True
become_method = sudo
